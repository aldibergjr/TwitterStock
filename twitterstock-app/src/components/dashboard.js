import React from 'react'
//import { createBrowserHistory } from 'history';
//const history = createBrowserHistory();

export default function Dashboard(){
    return(
        <div style={{height:"90vh",display:"flex" ,background:"white",padding:"3%"}}>
            <h1>Dashboard</h1>
        </div>
    )
}